#include <stdio.h>
/**
 * main - C Program that prints with the printf function
 * Return: The return value should be 0 for successful execution
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
