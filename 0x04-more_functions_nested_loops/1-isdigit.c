#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9)
 *
 * @c: parameter
 * Return: 1 for a number and 0 if not
 */
int _isdigit(int c)
{
	if (c >= 43 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
